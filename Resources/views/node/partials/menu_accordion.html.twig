{# TODO: handle errors in accordion #}
{% embed '@TunaCMSAdmin/_partials/accordion.html.twig' with {
label: 'Menu',
display_error: not form.translations.defaultLocale[app.request.defaultLocale].label.vars.valid
} %}
    {% block content %}
        {{ a2lix.translations(form.translations, ['label', 'url']) }}
        {{ form_row(form.clickable) }}
        {{ form_row(form.displayingChildren) }}
    {% endblock %}
{% endembed %}
