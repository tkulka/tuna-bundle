{% set menuHtml %}
    {% for item in menu.children %}
        {% include templates['menu_item'] with { item: item } %}
    {% endfor %}
{% endset %}
{% if options.wrap %}
    <ul class="menu menu--{{ name|lower }}">{{ menuHtml }}</ul>
{% else %}
    {{ menuHtml }}
{% endif %}


